# Catch-all: send every request to the Express serverless function (path preserved via :splat)
# Must be in public/ so it is deployed; works together with netlify.toml
/*    /.netlify/functions/server/:splat   200
